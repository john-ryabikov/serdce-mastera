export interface IPopup {
    showPopup: boolean,
    openPopup: () => void,
    closePopup: () => void
}

export interface IMenu {
    showMenu: boolean,
    openMenu: () => void,
    closeMenu: () => void
}